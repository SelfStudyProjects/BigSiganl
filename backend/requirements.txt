asgiref==3.9.1
contourpy==1.3.3
cycler==0.12.1
Django==5.2.5
django-cors-headers==4.3.1
djangorestframework==3.14.0
fonttools==4.60.0
kiwisolver==1.4.9
matplotlib==3.10.6
numpy==2.3.3
packaging==25.0
pandas==2.3.2
pillow==11.3.0
pyaes==1.6.1
pyasn1==0.6.1
pyparsing==3.2.5
python-dateutil==2.9.0.post0
python-decouple==3.8
python-dotenv==1.1.1
pytz==2025.2
rsa==4.9.1
seaborn==0.13.2
six==1.17.0
sqlparse==0.5.3
Telethon==1.41.2
tzdata==2025.2
gunicorn==21.2.0
dj-database-url==2.1.0
#psycopg2-binary==2.9.9  # commented out for Windows dev (pg_config missing)

whitenoise==6.6.0

# Use psycopg (v3) with the binary wheel on production so we don't need pg_config during install
psycopg[binary]

# If installation fails on Windows because pg_config is missing, either:
#  - Install PostgreSQL client/devel and ensure pg_config is on PATH, OR
#  - Install psycopg2-binary separately on a system that has pg_config, OR
#  - Comment out the psycopg2-binary line above and install it on the production server where PostgreSQL is available.